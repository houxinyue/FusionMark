[project]
name = "agent-copilot"
version = "0.1.0"
description = "Standalone copilot module for FusionMark"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.136.0",
    "uvicorn>=0.44.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
